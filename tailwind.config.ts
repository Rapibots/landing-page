import type { Config } from 'tailwindcss';

// Tailwind v4 primarily uses @theme in CSS. We set dark mode to class here
// so you can toggle <html class="dark"> at runtime.
export default {} satisfies Config;
